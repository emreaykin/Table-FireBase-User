let row = 3;
      
for (let i = 0; i < row; i++) {
  let satir =
  "<tr>"+

  "<td class='name'>...</td>"+
  "<td class='lastname'>...</td>"+
  "<td class='age'>...</td>"+
  
  "</tr>";

  $(".table").append(satir);
}